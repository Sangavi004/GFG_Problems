//Task to find that the large number is divisible by 13 or not
class Solution {
    public boolean divby13(String s) {
        int num=0;
        for(int i=0;i<s.length();i++){
            num=(num*10 + s.charAt(i)-'0')%13;
        }
        return num==0;
    }
}
